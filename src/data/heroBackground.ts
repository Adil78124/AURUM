import { interiorPhotoPaths } from "./interior";

/** Hero на главной и «Доставка» — тот же кадр, что блок «Общий зал» на странице интерьера. */
export const HERO_BACKGROUND_IMAGE = interiorPhotoPaths[4824];
